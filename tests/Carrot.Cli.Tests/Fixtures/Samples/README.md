# Future acceptance fixtures

The `Documents`, `Archives`, and `ApiResponses` directories reserve sanitized deterministic fixtures for future extractor, archive-defense, contract, and end-to-end tests. No operational fixtures are included in the layout-only phase.
